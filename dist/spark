#!/bin/bash
if [[ "$OSTYPE" == "linux-gnu" ]]; then
  os=linux
  distro_id=$(cat /etc/os-release | grep "^ID=" | cut -d'=' -f2)
  if [ $distro_id = "fedora" ]; then
    distro=fedora
  elif [ $distro_id = "debian" ]; then
    distro=debian
  elif [ $distro_id = "raspbian" ]; then
    distro=raspbian
  else
    echo "unrecognized linux distro"
  fi
else
  echo "unrecognized OSTYPE" $OSTYPE
fi


#!/bin/bash
echo "Welcome to Logan's world renowned computer configuration system!"
apt -qq update
# apt -qq upgrade --yes
# apt -qq install neofetch --yes
if [[ "$OSTYPE" == "linux-gnu" ]]; then
  os=linux
  distro_id=$(cat /etc/os-release | grep "^ID=" | cut -d'=' -f2)
  if [ $distro_id = "fedora" ]; then
    distro=fedora
  elif [ $distro_id = "debian" ]; then
    distro=debian
  elif [ $distro_id = "raspbian" ]; then
    distro=raspbian
  else
    echo "unrecognized linux distro"
  fi
else
  echo "unrecognized OSTYPE" $OSTYPE
fi

echo $os
echo $platform
base_url="https://files.loganhillyer.me"

# source <(curl -s $base_url/utils)


echo $distro
echo $os

case $distro in
  debian)
    echo "it's a debian system"
esac
